# econte (Python)

pydantic mirror of the econte storyboard schema, plus the CLI
(`validate` / `compile` / `run` / `ingest` / `sheet`) and the
manifest-driven ComfyUI runners. See the
[repository root README](../../README.md) for the project overview.

**Placeholder** — implementation lands across Phases P1–P3 of
`docs/implementation-plan.md`.
