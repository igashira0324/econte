# econte storyboard schema

**This file is a placeholder.** `SCHEMA.md` and `econte.schema.json` in this
directory are generated artifacts — see [`packages/econte`](../packages/econte/)
for the TypeScript/Zod source of truth. Do not hand-edit `econte.schema.json`;
run `npm run build:schema` in `packages/econte` instead.

Full field-by-field documentation lands in Phase P1 of the implementation
plan, alongside the Zod schema itself.
